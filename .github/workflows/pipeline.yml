name: 🔐 Pipeline DevSecOps

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

env:
  API_KEY: ${{ secrets.API_KEY }}
  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

jobs:
  pipeline:
    name: Build, Segurança e Deploy
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      # ✅ PASSO 1: Já está pronto — Baixar o código
      - name: 📥 Checkout do Código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ✅ PASSO 2: Já está pronto — Build.
      - name: ⚙️ Build
        run: |
          echo "Verificando arquivos..."
          ls src/

          echo "🔐 Injetando secrets do GitHub..."

          # Substituição usando os placeholders que você colocou no JS
          sed -i 's|{{API_KEY_PLACEHOLDER}}|'"$API_KEY"'|g' src/script.js
          sed -i 's|{{DB_PASSWORD_PLACEHOLDER}}|'"$DB_PASSWORD"'|g' src/script.js

          echo "✅ Secrets injetadas!"

          # Verificação (mostra se funcionou)
          echo "📋 Conteúdo após injeção:"
          grep -E "API_KEY|DB_PASSWORD" src/script.js

          echo "✅ Build finalizado!"

          echo "Build OK!"

      # ✅ PASSO 3: IMPLEMENTE AQUI — Secrets Scanning com Gitleaks
      # Dica: use a action oficial "gitleaks/gitleaks-action@v2"
      - name: 🔑 Secrets Scanning
        # run: echo "TODO - implemente o Secrets Scanning com Gitleaks aqui"
        # SUBSTITUA o run acima pela implementação correta com "uses:"
        #------------------
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        #------------------

      # ✅ PASSO 4: IMPLEMENTE AQUI — SAST com Semgrep
      # Dica: instale com "pip install semgrep" e rode "semgrep scan --config auto --error src/"
      - name: 🔍 SAST - Semgrep
        #run: echo "TODO - implemente o SAST com Semgrep aqui"
        # SUBSTITUA pelo comando real: pip install semgrep && semgrep scan --config auto --error src/
        #------------------
        #run: |
        #  pip install semgrep
        #  semgrep scan --config auto --error src/
        #------------------
        run: |
            pip install semgrep

            echo "🔍 Running Semgrep scan (SARIF)..."
            semgrep ci \
              --config p/default \
              --config p/security-audit \
              --config p/owasp-top-ten \
              --config p/cwe-top-25 \
              --sarif --output semgrep-results.sarif || true

            echo "📋 Findings (texto legível):"
            semgrep scan \
              --config p/default \
              --config p/security-audit \
              --config p/owasp-top-ten \
              --config p/cwe-top-25 \
              --text src/
            SCAN_EXIT=$?

            echo "❌ Falhando o step por causa dos findings acima"
            exit $SCAN_EXIT
              
      # ✅ PASSO 5: IMPLEMENTE AQUI — SCA com Grype
      # Dica: instale com "curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin"
      # e rode "grype dir:. --fail-on medium"
      - name: 📦 SCA - Grype
        #run: echo "TODO - implemente o SCA com Grype aqui"
        # SUBSTITUA pelo comando real de instalação + execução do Grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
          grype dir:. --fail-on medium

      # ✅ PASSO 6: Já está pronto — Deploy (só roda se tudo passar)
      - name: 🌐 Configurar GitHub Pages
        uses: actions/configure-pages@v4

      - name: 📤 Upload para Produção
        uses: actions/upload-pages-artifact@v3
        with:
          path: './src'

      - name: 🚀 Deploy em Produção
        id: deployment
        uses: actions/deploy-pages@v4
